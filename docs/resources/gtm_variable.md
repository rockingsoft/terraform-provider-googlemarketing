# googlemarketing_gtm_variable

Typed GTM variable resource.

Supports constant, data layer, first-party cookie, and custom JavaScript parameter fields through `value`, `data_layer_name`, `cookie_name`, and `javascript`.
