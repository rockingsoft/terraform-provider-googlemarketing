# googlemarketing_gtm_google_tag_config

Manages a Google Tag Manager Google tag configuration.

The resource sends a GTM `gtag_config` payload with `type = "google"` and a `tagId` template parameter. Run `TestAccGTMGoogleTagConfigPayloadRealAPI` with `GOOGLEMARKETING_ACC=1` to validate the payload against a real GTM workspace.
