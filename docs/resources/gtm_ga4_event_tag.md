# googlemarketing_gtm_ga4_event_tag

Manages a GA4 event tag in Google Tag Manager.

Use this resource for GA4 events together with `googlemarketing_gtm_google_tag_config` for the base Google tag. By default it does not serialize a measurement ID. `measurement_id_override` is available only for transitional legacy cases.
