# googlemarketing_gtm_tag

Typed GTM tag resource for legacy and escape-hatch tag types.

For GA4 events prefer `googlemarketing_gtm_ga4_event_tag`. For `type = "gaawe"`, `measurement_id` is serialized as the legacy GTM `measurementIdOverride` parameter for backwards compatibility.
