# googlemarketing_gtm_trigger

Typed GTM trigger resource.

Supports common trigger fields including `custom_event_name`, optional filter variable/operator/value, notes, and remote drift readback for the exposed schema fields.
